extends layout.pug

block title
  title #{title} // #{name}

block main
  article
    h1.article-title
      = title
      span.article-date= date.toLocaleDateString("en", { year: "numeric", month: "long", day: "numeric" })
    != content

